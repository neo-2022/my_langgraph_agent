# my_langgraph_agent

Локальный LangGraph-агент (ReAct) с Ollama + удобные локальные панели управления и React UI.
Проект сделан так, чтобы:
- локальная часть (Ollama) жила в одном виртуальном окружении без “облачных” зависимостей;
- облачные провайдеры (OpenAI/Anthropic и т.д.) позже добавлялись в отдельном окружении, чтобы не ловить конфликты пакетов.

---

## Быстрый старт (одна команда)

Из корня проекта:

```bash
~/my_langgraph_agent/run.sh


Скрипт поднимет (или оставит запущенными) сервисы в tmux:

LangGraph API

Settings UI (настройки/модели)

React UI (основной UI)

Адреса (что где открывать)

LangGraph API: http://127.0.0.1:2024

Документация API: http://127.0.0.1:2024/docs

Settings UI (FastAPI): http://127.0.0.1:8088

React UI (Vite): http://127.0.0.1:5174

UI Proxy (проксирует /api/* и /ui/* в React): http://127.0.0.1:8090

В React UI запросы к LangGraph идут через /api/..., а модели/настройки через /ui/....

Как смотреть логи (tmux)

Посмотреть логи конкретного сервиса:

tmux attach -t langgraph
tmux attach -t settings_ui
tmux attach -t ui_proxy
tmux attach -t ui

Выйти из tmux (не останавливая сервис): Ctrl+B затем D.

Как остановить сервисы

Остановить конкретную tmux-сессию:

tmux kill-session -t langgraph
tmux kill-session -t settings_ui
tmux kill-session -t ui_proxy
tmux kill-session -t ui

Проверить запущенные:

tmux ls
Как выбрать модель Ollama

Вариант 1 (рекомендуется):

Открой Settings UI: http://127.0.0.1:8088

Выбери модель → Сохранить выбор

Нажми Перезапустить LangGraph сервер

Обнови страницу (или React UI)

Вариант 2 (через React UI):

Открой http://127.0.0.1:5174

Панель Локальные модели (Ollama) → выбери модель → Сохранить → (если нужно) Перезапустить LangGraph

Структура проекта (важное)

agent/ — LangGraph проект (Python)

ui/ — React UI (Vite)

run.sh — запуск всего одной командой

ROADMAP.md — план развития

TASKS.md — текущие задачи/статус

ВАЖНО: как продолжать работу в новом чате

Если чат будет новый, просто начни так:

“Продолжаем проект my_langgraph_agent”

“Текущая задача: …”

(по желанию) вставь вывод:

tmux ls

~/my_langgraph_agent/run.sh

или git log -1 --oneline

Вся история зафиксирована в репозитории (код + README/TASKS/ROADMAP), поэтому продолжать можно без старой переписки.


---

## 2) TASKS.md
**Путь:** `~/my_langgraph_agent/TASKS.md`

Если хочешь — я могу обновить и его (чтобы было “что сделано / где остановились / что дальше”), но ты просил сейчас “сразу готовый” — тогда вставь вот это, оно компактно и пригодно для нового чата:

```md
# TASKS — текущий статус

## Что уже сделано (факт)
- Развернут LangGraph проект (ReAct) в `~/my_langgraph_agent/agent`.
- Виртуальное окружение: `~/my_langgraph_agent/venv` (Python 3.12).
- Убраны конфликтующие зависимости (protobuf-конфликты и лишние пакеты).
- Запуск LangGraph dev-сервера на `127.0.0.1:2024` работает.
- Добавлен Ollama (`langchain-ollama`), проверено: модели отвечают.
- Добавлены инструменты:
  - `list_ollama_models`
  - `set_ollama_model`
  - `probe_tool_calls`
  - `probe_all_models_tool_calls`
- Settings UI (FastAPI) на `127.0.0.1:8088`:
  - выбор модели Ollama,
  - сохранение в `.env`,
  - кнопка “перезапустить LangGraph сервер” через tmux.
- React UI (Vite) в `ui/` на `127.0.0.1:5174`:
  - левый rail с выезжающими панелями,
  - панель “Локальные модели” читает `/ui/models`.
- UI Proxy (`agent/src/react_agent/ui_proxy.py`) на `127.0.0.1:8090`:
  - проксирует `/api/*` (LangGraph API) и `/ui/*` (модели/настройки) для React UI.
- `run.sh` поднимает сервисы в tmux одной командой.

## Где остановились
- UI: базовая архитектура панелей сделана.
- Следующий этап: интерфейс графов (визуализация узлов/ребер, запуск runs, просмотр state/диффов, таймлайн).

## Что дальше (план ближайших шагов)
1) UI “Графы”:
   - список assistants,
   - получение графа (`/assistants/{id}/graph`),
   - рендер узлов/ребер (пока простой).
2) UI “Запуски”:
   - запуск `runs/wait`,
   - история, таймлайн, просмотр сообщений.
3) State/диффы:
   - отображение state по шагам (минимально),
   - сравнение двух состояний.
4) Инструменты:
   - импорт tools_fs/tools_shell из старого проекта,
   - подключение к графу через ToolNode.
5) В будущем:
   - Memory (долговременная память),
   - Retrieval (векторная база/документы),
   - Web search (Tavily или альтернатива),
   - Open Interpreter (узел для генерации кода/инструментов),
   - облачные модели в отдельном venv.

## Команды-памятки
- Запуск всего: `~/my_langgraph_agent/run.sh`
- Логи: `tmux attach -t langgraph|settings_ui|ui_proxy|ui`
- Список сессий: `tmux ls`